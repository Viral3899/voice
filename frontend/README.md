# Frontend for Voice AI Appointment Agent

A minimal React + Vite frontend for the healthcare voice agent.

## Start

```bash
cd frontend
npm install
npm run dev
```

The frontend proxies `/api` requests to the backend at `http://127.0.0.1:8000`.
